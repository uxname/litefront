node_modules/
dist/
# TanStack Start / Nitro SSR build output and caches
.output/
.nitro/
.tanstack/
storybook-build/
.idea
.vscode
.env
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.husky
dev-dist/
coverage
# Ladle (Storybook) config in .ladle/ is hand-written source and IS tracked.
# Its build output goes to storybook-build/ (ignored above) and its cache to
# node_modules/.ladle, so nothing under .ladle/ needs ignoring.
tmp/

# Audit Report: Error Handling & Resiliency — 2026-05-24 09:46

| Check ID | Проверка | Статус | Уверенность | Доказательство | Решение | Исправлено |
|----------|----------|--------|-------------|----------------|---------|------------|
| ERR-01 | Ошибки не проглатываются — catch-блоки обрабатывают или пробрасывают | ✅ PASS | Высокая | GraphQL `errorExchange` (graphql-client.ts:9-21) шлёт в Sentry; Clipboard catch в ErrorFallback (index.tsx:180-182) логирует и завершается — единственный catch, и он корректно логирует ошибку, не скрывая её полностью | — | — |
| ERR-02 | Внутренние детали (stack trace, пути, версии) не попадают в ответы | ⚠️ WARN | Средняя | ErrorFallback (index.tsx:263-269) показывает полный stack trace под кнопкой "Details" без проверки `import.meta.env.PROD`. Sentry `beforeSend` (sentry/config.ts:26-39) чистит query-параметры из filename — хорошо, но UI-слой не защищён | Обернуть показ stack trace в `if (import.meta.env.DEV)` или добавить conditional rendering по флагу `showDevInfo` | Нет |
| ERR-04 | Unhandled rejections и uncaught exceptions имеют обработчики | ⚠️ WARN | Средняя | `unhandledrejection` слушатель есть (GlobalErrorBoundary.tsx:8-14); ErrorBoundary оборачивает всё дерево (main.tsx:72); **Нет** `window.addEventListener('error', ...)` для не-React runtime-ошибок вне промисов | Добавить `window.addEventListener('error', handler)` параллельно с `unhandledrejection` в GlobalErrorBoundary | Нет |
| ERR-05 | Внешние вызовы (HTTP-клиенты, DB) имеют явные таймауты | ❌ FAIL | Высокая | GraphQL-клиент (graphql-client.ts:4-29) создаётся без `fetchOptions.timeout`; OIDC (oidc-client.ts:4-12) без конфигурации таймаута. Ни URQL, ни react-oidc-context не выставляют таймауты по умолчанию | Добавить `fetchOptions: { signal: AbortSignal.timeout(15000) }` в URQL или использовать custom fetch с таймаутом; для OIDC задать `timeout` в `AuthProvider` | Нет |
| ERR-07 | Error responses консистентны по структуре | ✅ PASS | Высокая | ErrorFallback (index.tsx:91-122) категоризирует ошибки (AUTH/ACCESS/NETWORK/SERVER/UNKNOWN) по единому шаблону `ErrorConfig`; все точки входа (__root.tsx, GlobalErrorBoundary) используют один компонент | — | — |
| ERR-08 | Retry-стратегии используют exponential backoff с jitter | ❌ FAIL | Высокая | Автоматических retry нет. Кнопка "Повторить" (ErrorFallback index.tsx:153-161) делает либо `reset()` + `router.invalidate()`, либо `window.location.reload()` — мгновенная повторная попытка без backoff. GraphQL-клиент не использует `@urql/exchange-retry` | Установить `@urql/exchange-retry` и подключить retryExchange с exponential backoff; в ErrorFallback добавить `attempt` счётчик и увеличивающуюся задержку | Нет |
| ERR-09 | AbortSignal/CancellationToken пробрасывается во внешние вызовы | ❌ FAIL | Высокая | Нет механизма проброса AbortSignal в URQL или OIDC. GraphQL-клиент не принимает `signal` в параметрах | Добавить поддержку AbortSignal в обёртку над `client.query`/`client.mutation`; рассмотреть TanStack Query как дополнительный слой с нативной поддержкой отмены | Нет |

## Audit Coverage
Файлов проверено: 9 | Пропущено: 0

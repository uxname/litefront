# Audit Report: Architecture & File Structure — 2026-05-24 09:46

| Check ID | Проверка | Статус | Уверенность | Доказательство | Решение | Исправлено |
|----------|----------|--------|-------------|----------------|---------|------------|
| ARC-01 | Бизнес-логика вынесена из route handlers в service/domain слой | PASS | 90% | Все route handlers (__root.tsx, callback.tsx, protected/index.tsx) делегируют логику компонентам или ограничиваются навигационными гардами. `beforeLoad` в `routes/protected/index.tsx:5-10` — корректный роутинг-гард, не бизнес-логика. | Нет | — |
| ARC-02 | Presentation layer не взаимодействует с БД напрямую | PASS | 95% | Прямых обращений к БД нет. `pages/home/ui/index.tsx:62` использует `useGetCountryQuery` — сгенерированный URQL-хук (GraphQL-клиент). Это стандартный паттерн data-access через shared/api слой. | Нет | — |
| ARC-03 | Нет circular dependencies между модулями | PASS | 95% | Все импорты следуют FSD-правилу: app → pages → widgets → features → entities → shared. `shared/api` → `shared/lib` (внутри слоя). `features/auth` не импортирует entities/widgets. `entities/counter` не импортирует features/widgets. | Нет | — |
| ARC-04 | Нет god-объектов: файлы и классы имеют единственную ответственность | PASS | 85% | Все файлы соблюдают SRP. Самый крупный — `pages/home/ui/index.tsx` (332 строки), но это композитная страница с несколькими секциями (hero, features grid, playground), что допустимо для страницы. `shared/ui/ErrorFallback/index.tsx` (278 строк) содержит error categorization + normalization + UI — можно выделить, но это не god-object. | Опционально: вынести `detectErrorCategory` и `normalizeError` в `shared/lib/errors` | Нет |
| ARC-05 | Конфигурация и env-переменные изолированы в config-модуле | **FAIL** | 95% | `shared/config/env.ts` определён, но `import.meta.env` используется напрямую в: (1) `features/auth/api/oidc-client.ts:5-9` — VITE_OIDC_*; (2) `shared/api/graphql-client.ts:6` — VITE_GRAPHQL_API_URL; (3) `shared/lib/sentry/config.ts:4,10,12,22` — VITE_SENTRY_DSN, MODE, VITE_APP_VERSION, PROD; (4) `main.tsx:68` — VITE_MOCK_AUTH. | Заменить прямые `import.meta.env` на импорт из `@shared/config/env` | Нет |
| ARC-07 | Доменный слой не импортирует инфраструктурные модули | PASS | 95% | `entities/counter/model/store.ts` использует Zustand (инфраструктура), но в рамках model-сегмента entities это нормированно — Zustand выбран как shared-level state manager. Ни один файл entities не импортирует из features/pages/widgets. Типы в `entities/counter/model/types.ts` не имеют зависимостей. | Нет | — |

## Audit Coverage
Файлов проверено: 20 | Пропущено: 0

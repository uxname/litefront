{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits",
    "group:allNonMajor"
  ],
  "labels": ["dependencies"],
  "prConcurrentLimit": 5,
  "prHourlyLimit": 2,
  "packageRules": [
    {
      "description": "Flag major updates for manual review — there is no CI (meta ADR-0001), so run `task check` / `npm run check` locally before merging one.",
      "matchUpdateTypes": ["major"],
      "addLabels": ["major-update"]
    }
  ]
}

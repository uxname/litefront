query AllFilms($first: Int!) {
    allFilms(first: $first) {
        edges {
            node {
                id
                title
            }
        }
    }
}

query AllPlanets($first: Int!) {
    allPlanets(first: $first) {
        edges {
            node {
                id
                name
            }
        }
    }
}
